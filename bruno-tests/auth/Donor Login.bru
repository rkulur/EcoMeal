meta {
  name: Donor Login
  type: http
  seq: 7
}

post {
  url: http://localhost:3000/api/v1/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "donor@example.com",
    "password": "securepass"
  }
}

script:post-response {
  bru.setVar("donorToken",res.body.token)
}
